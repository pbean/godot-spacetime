# Smoke Test Module Placeholder

This directory is reserved for the future SpacetimeDB module that will back the supported smoke-test and quickstart workflow.

It intentionally contains no module implementation yet. Story `1.2` only establishes the canonical location so validation and documentation can reference one stable path.
