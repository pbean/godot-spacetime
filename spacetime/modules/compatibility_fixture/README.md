# Compatibility Fixture Placeholder

This directory is reserved for the future SpacetimeDB module that will exercise schema drift, compatibility validation, and regeneration checks.

It intentionally contains no fixture implementation yet. Story `1.2` establishes the location so later codegen and release-hardening stories can extend the same path instead of inventing a new one.
